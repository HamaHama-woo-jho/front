import React from 'react';
import AppLayout from '../components/AppLayout';

const profile = () => {
  const word = '준비 중입니다.';
  return (
    <AppLayout>
      {word}
    </AppLayout>
  );
};

export default profile;
